import { ConfirmpswDirectiveDirective } from './confirmpsw-directive.directive';

describe('ConfirmpswDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmpswDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
